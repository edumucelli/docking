[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "docking"
version = "1.16.0"
description = "A lightweight, feature-rich dock for Linux written in Python with GTK 3 and Cairo"
requires-python = ">=3.10"
license = {text = "GPL-3.0-or-later"}
dependencies = [
    "openmeteo-requests>=1.2",
    "requests-cache>=1.0",
    "retry-requests>=2.0",
]

[project.scripts]
docking = "docking.app:main"
docking-camshield-helper = "docking.applets.camshield.helper:main"

[project.optional-dependencies]
dev = [
    "behave>=1.2.6",
    "cohesion>=1.2.0",
    "import-linter>=2.9",
    "lizard>=1.17",
    "Pillow>=11.0",
    "pytest>=7.0",
    "pytest-cov",
    "radon>=6.0",
    "ruff==0.15.12",
    "scikit-image>=0.25",
    "ty==0.0.34",
    "vulture>=2.16",
]

[tool.setuptools.packages.find]
include = ["docking*"]

[tool.setuptools.package-data]
docking = [
    "assets/*.svg",
    "assets/*.png",
    "assets/clock/Default/*.svg",
    "assets/clock/Default24/*.svg",
    "assets/icons/*.png",
    "assets/icons/applets/*.png",
    "assets/themes/*.json",
    "assets/weather/*.csv.gz",
    "assets/sounds/*.ogg",
    "assets/sounds/*.md",
    "assets/history/*.json",
    "assets/stretch/*.json",
    "locale/*.pot",
    "locale/*/LC_MESSAGES/*.mo",
    "locale/*/LC_MESSAGES/*.po",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = '-m "not visual"'
markers = [
    "visual: screenshot-based visual regression tests",
]


[tool.coverage.run]
omit = [
    "docking/platform/*",
    "docking/ui/preview.py",
    "docking/ui/renderer.py",
]

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "B", "UP", "SIM", "C4", "RUF", "PIE", "RET", "ISC", "PTH"]
ignore = [
    "E402",   # gi.require_version() must precede gi.repository imports
    "RUF001", # allow existing non-ASCII UI/test glyphs
    "RUF002", # avoid broad docstring punctuation churn
    "RUF003", # avoid broad comment punctuation churn
]  

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["E501"]  # test data strings can exceed line length
"tests/applets/test_weather_api.py" = ["N802"]

[tool.ty.environment]
python-version = "3.10"

[tool.ty.rules]
unresolved-import = "ignore"  # gi/cairo lack type stubs
unresolved-attribute = "ignore"  # gi.repository dynamic attrs
# openmeteo-requests has incomplete type stubs
invalid-argument-type = "warn"
no-matching-overload = "warn"

[tool.vulture]
paths = ["docking", "tests"]
min_confidence = 100
sort_by_size = true

[tool.importlinter]
root_package = "docking"

[[tool.importlinter.contracts]]
name = "Core stays independent from applets, platform, and UI"
type = "forbidden"
source_modules = ["docking.core"]
forbidden_modules = [
    "docking.applets",
    "docking.platform",
    "docking.ui",
]
ignore_imports = [
    "docking.core.config -> docking.applets.identity",
    "docking.core.config -> docking.platform.launcher",
]

[[tool.importlinter.contracts]]
name = "Platform does not import UI"
type = "forbidden"
source_modules = ["docking.platform"]
forbidden_modules = ["docking.ui"]

[[tool.importlinter.contracts]]
name = "Applets do not depend on UI internals"
type = "forbidden"
source_modules = ["docking.applets"]
forbidden_modules = ["docking.ui"]
ignore_imports = [
    "docking.applets.bluetooth.render -> docking.ui.overlays",
    "docking.applets.camshield.render -> docking.ui.overlays",
    "docking.applets.micshield.render -> docking.ui.overlays",
    "docking.applets.music.render -> docking.ui.overlays",
    "docking.applets.notifications.render -> docking.ui.overlays",
    "docking.applets.popup -> docking.ui.display",
    "docking.applets.stretchcoach.render -> docking.ui.overlays",
]
