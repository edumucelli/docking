"""Applet metadata for the Mic Shield applet."""

from __future__ import annotations

from docking.applets.identity import AppletCategory, AppletMeta

meta = AppletMeta(
    id="micshield",
    name="Mic Shield",
    category=AppletCategory.SYSTEM,
)
