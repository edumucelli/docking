#!/bin/sh
set -eu

case "${1:-}" in
    remove|purge)
        if [ -f /usr/lib/docking/refresh-desktop-caches ]; then
            sh /usr/lib/docking/refresh-desktop-caches
        fi
        ;;
esac

exit 0
